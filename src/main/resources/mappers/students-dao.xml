<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.aliyun.learnjava.dao.StudentsDao">
        <resultMap id="studentsMap" type="com.aliyun.learnjava.entity.Students">
        <id property="id" column="id"></id>
        <result property="name" column="name"></result>
        <result property="gender" column="gender"></result>
        <result property="classId" column="class_id"></result>
</resultMap>

    <select id="getStudent" parameterType="long" resultMap="studentsMap">
        SELECT * FROM student WHERE id = #{id}
    </select>

    <select id="getStudentPagination" resultMap="studentsMap">
        SELECT * FROM student LIMIT #{limit} OFFSET #{offset}
    </select>

    <insert
        id="insertStudent"
        parameterType="com.aliyun.learnjava.entity.Students"
        useGeneratedKeys="true"
        keyProperty="id"
    >
        INSERT INTO student (name, gender, class_id) VALUES (#{student.name}, #{student.gender}, #{student.classId})
    </insert>

    <update
        id="updateClassId"
        parameterType="com.aliyun.learnjava.entity.Students"
    >
       UPDATE student SET name = #{student.name},gender = #{student.gender}, class_id = #{student.classId} WHERE id = #{student.id}
    </update>

    <delete
        id="deleteStudent"
        parameterType="long"
    >
       DELETE FROM student WHERE id = #{id}
    </delete>

</mapper>